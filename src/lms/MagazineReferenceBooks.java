package lms;

public class MagazineReferenceBooks extends Item {
	 public MagazineReferenceBooks(String title, int checkOutLength, double bookPrice, boolean canBeCheckedOut, int copyNumber) {
	        super(title, checkOutLength, bookPrice, canBeCheckedOut, copyNumber);
	    }
}
