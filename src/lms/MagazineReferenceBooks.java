package lms;

public class MagazineReferenceBooks extends Item {
	 public MagazineReferenceBooks(String title, int checkOutLength, double magazineRefPrice, boolean canBeCheckedOut, int copyNumber) {
	        super(title, checkOutLength, magazineRefPrice, canBeCheckedOut, copyNumber);
	    }
}
